return {
  defaults = {
    --- Whether to use volar
    use_volar = false,
  },
  schema = {
    use_volar = {
      type = "boolean",
      description = "Whether to use volar in place of a typescript server",
      default = false,
    }
  }
}
